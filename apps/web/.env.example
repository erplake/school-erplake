# Frontend environment variables
# Base URL for API (include protocol, no trailing slash)
VITE_API_BASE=http://localhost:8000
# Optional: Dev access token to auto-attach (Bearer). Leave blank in production.
VITE_DEV_ACCESS_TOKEN=
